%div{:id => "container"}
  - @categories.each do |category|
     
    %a{:href => categories_path(:root => category.name)}
      %div{:class => "categorybox"}
        %h1= category.name
        %div{:class => "categorytitle"}
          %h2= category.name
        %div{:class => "infos"}
        %div{:class => "categoryfooter"}
          
