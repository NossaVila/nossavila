#container
  -if(flash[:partial])
    = render partial: "shared/flashes/#{flash[:partial]}"
  .navcontainer
    %nav
      %table
        %tbody
          - @categories.each do |cat|
            %tr
              %td{:class => "btn"}
                %a{:href => services_path(:category => cat.id), :class => "link"}
                  %h3= cat.name
              
  .servicescontainer
    .btn
      %a{:href => new_service_path, :class => "link"}
        %h3 Criar novo Serviço
    %br
    - @services.each do |service| 
      %a{:href => service_path(service)}
        .servicecard
          %h2{:class => "cardtitle"}= service.title
          %p{:class => "carddescription"}= service.description
          %h4{:class => "cardowner"}= service.user.first_name
